/**
 * Punto de entrada principal (Entry Point) de nuestra aplicación Kotlin.
 *
 * En la Lección 1 aprendimos que la JVM buscará una función llamada 'main'
 * para comenzar a ejecutar nuestro código, al igual que un director de cine
 * necesita saber en qué escena empezar a rodar.
 */
fun main() {
    // println es una función de la librería estándar de Kotlin que imprime
    // el texto en la consola y añade un salto de línea al final.
    println("===============================")
    println("🤖 Welcome to AI Chat CLI")
    println("===============================")
}
